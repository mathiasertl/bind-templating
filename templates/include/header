$TTL      3600\
@       IN      SOA     ns1.{{ zone }}. hostmaster.{{ zone }}. (\
                   {{ serial }}           ; Serial\
                        {{ soa.refresh }}           ; Refresh [1h]\
                        {{ soa.retry }}           ; Retry   [10m]\
                        {{ soa.expire }}           ; Expire  [1d]\
                        {{ soa.negative_ttl }} )         ; Negative Cache TTL [1h]\
;
